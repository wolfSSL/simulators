name: SDK test

on:
  push:
    branches: [main]
  pull_request:
    branches: ['**']
  workflow_dispatch:

jobs:
  sdk-test:
    name: NXP Plug&Trust SDK + OpenSSL cross-verification
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - name: Build sdk-test image
        uses: docker/build-push-action@v6
        with:
          context: SE050Sim
          file: SE050Sim/Dockerfile.sdk-test
          tags: se050-sdk-test:ci
          load: true
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Run sdk-test suite
        run: docker run --rm se050-sdk-test:ci
